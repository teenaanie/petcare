node_modules/
.env
dist/

# Local Netlify folder
.netlify

# Raw scraped provider data — contains business contact details and is large.
# Kept local only; import via scripts/import-providers.mjs.
data/

# Claude Code local state. worktrees/ especially must never be committed —
# each one is a full checkout of this same repo.
.claude/
